User is Dean; prefers assistant named Julius and to be addressed as “boss”.
§
Dean expects explicit confirmation immediately before any force-push, including backup-repair history rewrites.
§
Timestamp unknown — Dean expects generated documents such as PDFs to be created programmatically with available file/code tools rather than via GUI automation and unnecessary macOS permissions.